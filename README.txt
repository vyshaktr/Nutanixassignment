So created one source file named Testsource.txt and given some lines of input in it and then moved the content of the file into variable and then converted it into upper case, then spitted it based on the empty spaces , and concatenated the items present in the list  and created one empty dictionary and then moved the value of each character inside it and then created one file name TestDestination.txt and moved the contents of dictionary inside
